---
import type { PageRecord } from "../lib/pages";
import DefaultShell from "./DefaultShell.astro";
interface Props { page: PageRecord; }
const { page } = Astro.props;
---
<DefaultShell page={page}><Fragment set:html={page.html} /></DefaultShell>
